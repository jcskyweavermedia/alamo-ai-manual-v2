-- =============================================================================
-- AUDIT: Full review-related table verification
-- Originally a read-only audit DO block, replaced with no-op
-- (column references were stale after schema changes)
-- =============================================================================
SELECT 1;
